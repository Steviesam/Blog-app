import React from 'react';
import './Spinner.css'; // Assuming you want to add some custom styles

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
